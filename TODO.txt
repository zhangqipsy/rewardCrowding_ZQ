let's do everything else
        done! drawObjects for data.draw (circles, bars etc.)
                also support poly, rect
                fixation coor and width

BUG: collectResponse waits until key release
BUG: why there are some NaN rows in the Trials (nresp is skipping
through them)
